{% extends 'admin/base.html.twig' %}

{% block title %}Modifier un Post{% endblock %}

{% block body %}
    <section class="row">
        <div class="col-md-6 offset-3">
            <h2 class="display-5 my-4">Modifier l'article {{ painting.title }}</h2>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="modifier l'article" class="btn btn-outline-success mt-3">
            {{ form_end(form) }}
        </div>
    </section>
{% endblock %}
